DTrace consumer for Python based on libdtrace.

Currently this package provides two modules: one wraps libdtrace using ctypes.
The other one used cython.

WWW: http:// tmetsch.github.com/python-dtrace/
